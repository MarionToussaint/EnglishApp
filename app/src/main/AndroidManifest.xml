<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mmtou.englishapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FirstScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EasyActivity" />
        <activity android:name=".MediumActivity" />
        <activity android:name=".HardActivity" />
        <activity android:name=".Level1EasyActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".Level1MediumActivity" />
        <activity android:name=".Level1HardActivity" />
        <activity android:name=".ScoreActivity" />
        <activity android:name=".ScoreFinalActivity"></activity>
    </application>

</manifest>